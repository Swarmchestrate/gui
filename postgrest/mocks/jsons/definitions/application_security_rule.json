{
    "required": ["id", "protocol", "port_type"],
    "properties": {
        "id": {
            "description": "Note:\nThis is a Primary Key.<pk/>",
            "format": "integer",
            "type": "integer"
        },
        "application_microservice_id": {
            "description": "Note:\nThis is a Foreign Key to `application_microservice.application_microservice_id`.<fk table='application_microservice' column='id'/>",
            "format": "integer",
            "type": "integer"
        },
        "name": { "format": "text", "type": "string" },
        "protocol": {
            "enum": ["TCP", "UDP"],
            "format": "public.protocol_enum",
            "type": "string"
        },
        "port": { "format": "integer", "type": "integer" },
        "targetport": { "format": "integer", "type": "integer" },
        "port_type": {
            "enum": ["NodePort", "HostPort", "ContainerPort", "ClusterIP"],
            "format": "public.port_type_enum",
            "type": "string"
        },
        "cluster_ip": { "format": "character varying", "type": "string" }
    },
    "type": "object"
}
